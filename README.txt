contact email:matheus18.1@yahoo.com 

The "Compare_attribute" plugin is a tool developed for QGIS that allows users to compare two shapefiles and extract features with corresponding attributes. With an intuitive interface, this plugin simplifies the process of comparing and extracting geospatial data in the QGIS environment.

	Key Features:

Compare_attributes The plugin compares the attributes of selected columns between two shapefiles, allowing users to identify features with similar attributes.

Extraction of Corresponding Features: Based on the comparison of attributes, the plugin extracts features that have the same attributes in the selected columns, preserving their original geometry.

Consideration of Different Data Types: Effectively handles different types of data in selected columns, ensuring accurate comparisons even with different data types.

Extraction of Features with Unique Attributes: In addition to features with corresponding attributes, the plugin also allows the extraction of features that have unique attributes in relation to the other shapefile.

	Integration and Usability:

The "Shapefile Comparator" plugin is perfectly integrated into the QGIS environment, providing an intuitive and efficient user experience. Users can easily access the plugin's functionalities through the QGIS interface, simplifying the process of comparing and extracting geospatial data.


Make sure to customize the description according to the specific features and functionality of your plugin. This will help users quickly understand how the plugin can be useful in their QGIS workflows.

	How to use the plugin:

Select Shapefile 1 with a Shapefile with valid geometry, then select the column you want to compare, in the Shapefile 2 input you can select any type of file, not necessarily a Shapefile, then the column you want to compare, you don't need to worry about the type of the columns, because if they are of different types, the columns will be transformed into text type.

After that, fill in the Shapefile's common attributes output with a valid output, if you want the Shapefile's unique attributes file, just fill in with a valid output, if you leave it blank the file will not be generated.


/////Português

email para contato:matheus18.1@yahoo.com

O plugin Compare_attribute é uma ferramenta desenvolvida para o QGIS que permite aos usuários comparar dois shapefiles e extrair feições com atributos correspondentes. Com uma interface intuitiva, este plugin simplifica o processo de comparação e extração de dados geoespaciais no ambiente QGIS.

	Recursos Principais:

Compare_attributes plugin compara os atributos das colunas selecionadas entre dois shapefiles, permitindo aos usuários identificar feições com atributos semelhantes.

Extração de Feições Correspondentes: Com base na comparação dos atributos, o plugin extrai as feições que possuem os mesmos atributos nas colunas selecionadas, preservando sua geometria original.

Consideração de Tipos de Dados Diferentes: Lida de forma eficaz com diferentes tipos de dados nas colunas selecionadas, garantindo comparações precisas mesmo com tipos de dados distintos.

Extração de Feições com Atributos Únicos: Além das feições com atributos correspondentes, o plugin também permite a extração de feições que possuem atributos únicos em relação ao outro shapefile.

	Integração e Usabilidade:

O plugin "Comparador de Shapefiles" está integrado perfeitamente ao ambiente do QGIS, proporcionando uma experiência de usuário intuitiva e eficiente. Os usuários podem acessar facilmente as funcionalidades do plugin através da interface do QGIS, simplificando o processo de comparação e extração de dados geoespaciais.

	Como utilizar o plugin:

Selecione o Shapefile 1 com um Shapefile com geometria valida, em seguida selecione a coluna que deseja comparar, na entrada Shapefile 2 pode selecionar qualquer tipo de arquivo não necessariamente um Shapefile, em seguida a coluna que deseja comparar, não precisa se preocupar com o tipo das colunas, porque caso sejam de tipos diferentes as colunas seram transformadas no tipo texto.

Apos isso preencha a saída atributos comuns do Shapefile com uma saída valida, caso queira o arquivo dos atributos únicos do Shapefile basta preencher com uma saída valida, caso deixe em branco o arquivo não será gerado.

For more information, see the PyQGIS Developer Cookbook at:
https://github.com/MatheusOliveiraFreitas/compare_attributes

(C) 2011-2018 GeoApt LLC - geoapt.com

